export const getStargateOFTQuote = async () => {
  // if (
  //   !args ||
  //   !fromChain ||
  //   !toChain ||
  //   !selectedToken ||
  //   !sendValue ||
  //   !Number(sendValue) ||
  //   !toTokenInfo
  // ) {
  //   return;
  // }
  // try {
  //   const bridgeAddress = selectedToken.rawData.stargate?.bridgeAddress as `0x${string}`;
  //   const quoteOFTResponse = await publicClient?.readContract({
  //     address: bridgeAddress,
  //     abi: STARGATE_POOL,
  //     functionName: 'quoteOFT',
  //     args: [args] as any,
  //   });

  //   return { quoteOFT: quoteOFTResponse };
  // } catch (error: any) {
  //   // eslint-disable-next-line no-console
  //   console.log(error, error.message);
  // }
}